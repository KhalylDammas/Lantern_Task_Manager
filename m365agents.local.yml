# yaml-language-server: $schema=https://aka.ms/m365-agents-toolkits/v1.11/yaml.schema.json
# Visit https://aka.ms/teamsfx-v5.0-guide for details on this file
# Visit https://aka.ms/teamsfx-actions for details on actions
version: v1.11

provision:
  # Creates an app
  - uses: teamsApp/create
    with:
      # app name
      name: Lantern_Task_Manager${{APP_NAME_SUFFIX}}
    # Write the information of created resources into environment file for
    # the specified environment variable(s).
    writeToEnvironmentFile: 
      teamsAppId: TEAMS_APP_ID

  # Create or reuse an existing Microsoft Entra application for bot.
  - uses: aadApp/create
    with:
      # The Microsoft Entra application's display name
      name: Lantern_Task_Manager${{APP_NAME_SUFFIX}}
      generateClientSecret: true
      generateServicePrincipal: true
      signInAudience: AzureADMultipleOrgs
    writeToEnvironmentFile:
      # The Microsoft Entra application's client id created for bot.
      clientId: BOT_ID
      # The Microsoft Entra application's client secret created for bot.
      clientSecret: SECRET_BOT_PASSWORD
      # The Microsoft Entra application's object id created for bot.
      objectId: BOT_OBJECT_ID

  # Create or update the bot registration on dev.botframework.com
  - uses: botFramework/create
    with:
      botId: ${{BOT_ID}}
      name: Lantern_Task_Manager
      messagingEndpoint: ${{BOT_ENDPOINT}}/api/messages
      description: ""
      channels:
        - name: msteams


  # Build app package with latest env value
  - uses: teamsApp/zipAppPackage
    with:
      # Path to manifest template
      manifestPath: ./appPackage/manifest.json
      outputZipPath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip
      outputFolder: ./appPackage/build

  # Validate app package using validation rules
  - uses: teamsApp/validateAppPackage
    with:
      # Relative path to this file. This is the path for built zip file.
      appPackagePath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip

  # Apply the app manifest to an existing app in
  # Developer Portal.
  # Will use the app id in manifest file to determine which app to update.
  - uses: teamsApp/update
    with:
      # Relative path to this file. This is the path for built zip file.
      appPackagePath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip


deploy:
  # Generate runtime environment variables
  - uses: file/createOrUpdateEnvironmentFile
    with:
      target: ./.env
      envs:
        TEAMSFX_ENV: "local"
        CLIENT_ID: ${{BOT_ID}}
        CLIENT_SECRET: ${{SECRET_BOT_PASSWORD}}
        TENANT_ID: ${{TEAMS_APP_TENANT_ID}}
        BOT_SKIP_AUTH: "true"
        BOT_TYPE: ""
        TEAMS_APP_ID: ${{TEAMS_APP_ID}}
        TEAMS_APP_TENANT_ID: ${{TEAMS_APP_TENANT_ID}}
        LLM_PRIMARY: "groq"
        LLM_TOOL_PROFILE: "full"
        OPENAI_API_KEY: ${{SECRET_OPENAI_API_KEY}}
        GROQ_API_KEY: ${{SECRET_GROQ_API_KEY}}
        GROQ_MODEL: "openai/gpt-oss-120b"
        GROQ_FALLBACK_MODEL: "qwen/qwen3.6-27b"
        TURSO_DATABASE_URL: ${{SECRET_TURSO_DATABASE_URL}}
        TURSO_AUTH_TOKEN: ${{SECRET_TURSO_AUTH_TOKEN}}
        NOTIFICATION_ACTIVITY_ENABLED: "true"
        NOTIFICATION_BOT_DM_ENABLED: "true"
        LTM_VERIFIER_MODE: "created_by"
        LTM_CRON_SECRET: ${{SECRET_LTM_CRON_SECRET}}
        D365_ENVIRONMENT_URL: "https://lss-prod.operations.uae.dynamics.com"
        D365_CLIENT_ID: "fddfdd2c-9747-43bb-9895-58185732923d"
        D365_CLIENT_SECRET: ${{SECRET_D365_CLIENT_SECRET}}
        D365_TENANT_ID: "3151a94f-9459-4244-8099-04c1d93d92d6"
        D365_DATA_AREA_ID: "lsss"
