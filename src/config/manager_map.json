{
  "version": 1,
  "notes": "Optional assignee-to-verifier overrides. Production currently uses LTM_VERIFIER_MODE=created_by; add only verified Entra object IDs before enabling manager_map mode.",
  "managers_by_assignee_entra_id": {},
  "displays": {}
}
